# PRD · 网站收藏管家

> 版本：v1.1（第一版 + 功能修订）｜ 日期：2026-08-16 ｜ 状态：待确认
> 本文档是后续开发、布局、视觉的全部依据。
>
> **v1.1 修订记录（2026-08-16，需求讨论 + grilling 定稿）：**
> 1. 存储路径跨盘迁移：文件夹选择对话框 + 跨盘复制迁移 + 目标非空拒绝 + 失败回滚 + config.json 原子写入/损坏 .bak 恢复
> 2. 首次启动目录选择引导页
> 3. 导入/导出重构：MD 与 JSON 分开、系统文件对话框、JSON 完整备份仅覆盖导入、md 格式示例、导出成功状态栏提示
> 4. 选中行 hover 动效移除（选中行稳定高亮）
> 5. 弹窗拖拽误关闭修复（仅点击遮罩空白关闭）
> 6. 右键菜单位置修复（Teleport + 溢出翻转）
> 7. 分类创建入口（弹窗内新建 + 侧栏右键添加子分类/顶层分类 + 统一应用内弹窗替换 prompt/confirm）
>
> 说明：视觉风格（糖果像素）本次保持不变。

---

## 1. 产品目标和使用场景

### 1.1 一句话定义
一个**仅供本人使用**的本地桌面软件，用来管理收藏的实用网站，核心价值是**自动帮你发现"哪些网站已经打不开了"**，并方便地标记、清理失效网站。

### 1.2 使用场景
- 用户日常用 md 文件记录实用网站并分类，但**部分网站过一段时间会失效，且无法及时发现**。
- 打开软件即可看到全部网站的可用状态；一键"检测全部"，失效网站自动标红。
- 失效网站可一键移除（进回收站）或保留待复查；下次检测通过后标记自动恢复。

### 1.3 核心价值
1. **发现失效**：网站可访问性检测（这是区别于普通书签管理的核心功能）
2. **有序管理**：多层分类 + 标签 + 搜索，快速找到目标网站
3. **数据自持**：数据本地保存，可导出 md / 导入 md，不依赖任何联网服务

---

## 2. 技术栈

- **Tauri 2**（桌面应用外壳）+ **Rust 后端**（检测请求、JSON 文件读写）
- **Vue 3**（前端界面）+ **Pinia**（状态管理）
- 打包为安装程序，双击安装使用

---

## 3. 整体页面框架和导航结构

应用为**单页面（无多页跳转）**，一屏承载全部功能，通过左侧栏切换视图。

```
┌─────────────────────────────────────────────────────────────┐
│ 顶栏：Logo｜搜索框｜标签筛选▾｜检测全部｜＋添加｜导入/导出｜设置⚙ │
├──────────┬──────────────────────────────────────────────────┤
│ 侧边栏    │  主内容区（表格）                                 │
│ ── 分类 ── │  复选框│名称│链接│分类│标签│生命状态              │
│  全部(128) │  ────────────────────────────────               │
│  ▾开发工具 │  检测进度条（仅检测时出现）                       │
│    前端    │                                                  │
│    后端    │                                                  │
│  ▸设计资源 │                                                  │
│  未分类    │                                                  │
│ ── 视图 ── │                                                  │
│  ⚠ 失效(5)│                                                  │
│ ── 标签 ── │                                                  │
│  #前端     │                                                  │
│  #灵感     │                                                  │
│ ── 系统 ── │                                                  │
│  🗑 回收站  │                                                  │
├──────────┴──────────────────────────────────────────────────┤
│ 底部状态栏：共128个｜失效5｜未检测3｜上次检测 2026-08-15 10:30 │
└─────────────────────────────────────────────────────────────┘
```

### 导航关系
- **侧边栏即导航**：点击"分类"节点 → 主区只显示该分类下的网站；点击"⚠ 失效"→ 只显示失效网站；点击标签 → 只显示带该标签的网站；点击"全部"→ 显示所有。
- **"全部"是默认视图**，应用启动即进入。
- **顶栏是全局操作**，不随视图切换改变；主区内容随侧栏选择切换。
- 没有独立页面跳转；所有弹窗（添加/编辑、导入、设置、删除确认）为模态覆盖层。

---

## 4. 首页的内容结构

首页即主视图（"全部"视图）：

1. **顶栏**：Logo + 搜索框 + 标签筛选下拉 + 检测全部 + 添加网站 + 导入/导出 + 设置
2. **侧边栏**：分类树（可折叠）、⚠ 失效视图、标签列表、回收站
3. **主区表格**：复选框｜名称｜链接｜分类｜标签｜生命状态（♥♥♥ / ♥ / ♥?）
4. **底部状态栏**：总数 / 失效数 / 未检测数 / 上次检测时间
5. **批量操作条**：勾选多行后出现在表格上方
6. **检测进度条**：点"检测全部"后出现在表格上方，实时显示进度

---

## 5. 每个模块的用途

| 模块 | 用途 |
|---|---|
| **网站管理** | 增删改查网站信息；链接唯一防重；删除进回收站 |
| **分类体系** | 多层嵌套分类（最多 3 层），组织网站结构 |
| **标签** | 轻量标记，可筛选；编辑网站时自由输入、自动去重 |
| **可访问性检测** | 检测网站能否访问，标记失效，驱动清理流程 |
| **失效视图** | 集中浏览失效网站，批量复查 / 删除 |
| **回收站** | 软删除兜底，可恢复、可清空 |
| **搜索** | 按名称/链接/标签全局搜索 |
| **数据交换** | 本地 JSON 存储；md 导出 / 导入；存储位置迁移 |

---

## 6. 每个模块第一版最必要的数据和操作

### 6.1 网站管理
- **数据**：id、名称、链接、分类 id、标签数组、上次检测状态、上次检测时间
- **操作**：添加、编辑、删除（进回收站）、双击行编辑
- **规则**：链接唯一（相同链接禁止重复添加；名称可重复）

### 6.2 分类体系
- **数据**：id、名称、父分类 id（children 树）、层级（≤3 层）
- **操作**：新增、改名、删除、拖拽调整位置与层级
- **新增入口**：a) 添加/编辑网站弹窗的分类下拉 →"＋ 新建分类"（弹应用内小弹窗：选父级 + 输名称，创建后自动选中）；b) 侧栏右键"全部"→ 添加顶层分类；右键具体分类 → 添加子分类（放进该分类下）；第 3 层分类**不再显示"添加子分类"**
- **删除分类二选一**：a) 只删分类，网站挪到"未分类"；b) 连网站一起删，进回收站
- 所有分类操作的确认/输入统一使用**应用内弹窗**（与主题风格一致），不使用浏览器原生 prompt/confirm

### 6.3 标签
- **数据**：从网站标签数组自动汇总（仅用于筛选下拉）
- **操作**：编辑网站时自由输入，自动去重；无独立标签管理页

### 6.4 可访问性检测
- **数据**：每个网站的 `status`（ok / dead / unknown）+ `lastCheck`
- **操作**：检测全部 / 检测单个（悬停 ⋯ 菜单）/ 批量检测所选（批量条）
- **判断标准**：GET 请求跟随重定向，最终状态码 2xx/3xx = 可访问；404/403/5xx、超时、网络错误 = 失效
- **降级规则**：先测存的链接，失败后自动降级测根域名（避免子页面 404 误标）
- **断网保护**：检测前先探测公共连通性，不通则提示并中止
- **结果处理**：失效自动标红；下次检测通过自动恢复

### 6.5 失效视图
- **数据**：仅展示 `status = dead` 的网站
- **操作**：全选 → 检测所选（复查）/ 批量删除

### 6.6 回收站
- **数据**：删除的网站（保留原字段 + deletedAt）
- **操作**：恢复、彻底删除、清空回收站
- **规则**：回收站内网站不参与检测

### 6.7 搜索
- **操作**：顶部搜索框输入，按名称/链接/标签实时过滤主区

### 6.8 数据交换
- **格式**：MD 与 JSON 分开，弹窗提供四个按钮：导出 MD / 导出 JSON / 导入 MD / 导入 JSON
- **文件对话框**：所有导入/导出走系统文件对话框（Tauri dialog 插件）；导出保存对话框预填文件名 `网站收藏_<日期>.md/.json`（可改）；导入用打开对话框选文件
- **导出 MD**：生成 md 文件（标题树 + 链接列表 + 状态标记）；成功后状态栏闪现提示"已导出到 <路径>"（2-3s 消失）
- **导出 JSON**：生成完整数据备份（分类/网站/回收站/标签全部序列化）
- **导入 MD**：解析 md；弹窗内预选"覆盖"或"合并"模式；忽略状态标记（导入后视为未检测）
  - 合并规则：链接重复 → 更新名称/分类；分类不存在自动新建；不清空已有数据
  - 覆盖规则：清空重建；覆盖前自动备份当前数据为 .bak
- **导入 JSON**：仅覆盖（完整还原）；覆盖前自动备份当前数据为 .bak；执行前弹确认框
- **导入界面提供 md 格式示例**；JSON 导入显示说明文字（"导入程序导出的 .json 备份"）
- **迁移**：设置里"更改位置"→ 文件夹选择对话框选目录 → 移动文件（旧位置不留副本），可反复更换
  - 目标目录**非空则拒绝迁移**（提示"目标目录已有数据"）
  - 迁移失败**自动回滚**（删除已复制内容，源文件不动）
  - 设置弹窗显示**当前数据文件完整路径**（含文件名）

---

## 7. 首页和各模块之间的关系

```
搜索 ──过滤──▶ 主区表格（当前视图）
标签 ──筛选──▶ 主区表格
分类 ──筛选──▶ 主区表格
检测全部 ──▶ 主区表格行状态实时刷新 + 进度条 + 底部统计更新
失效视图 ──▶ 主区切换为"仅失效"子集
回收站 ──▶ 主区切换为回收站列表（恢复/清空/彻底删除）
添加/编辑/导入 ──▶ 写回数据 → 主区表格即时更新 → 即时写盘
```

- 所有模块的最终落点都是**主区表格**；侧栏决定"看哪个子集"，顶栏决定"做什么动作"。
- 底部状态栏始终反映**当前整个库**的统计，不随筛选变化。

---

## 8. 本地数据文件、备份和恢复要求

### 8.1 数据文件
- 单个 **JSON 文件**，默认存放于系统用户数据目录（Windows：`AppData\Roaming\<应用名>\data.json`）
- 数据目录**可迁移**到任意盘符/目录；迁移后的路径记录在 `config.json`
- 结构示例：

```json
{
  "version": 1,
  "categories": [
    { "id": "c1", "name": "开发工具", "children": [ { "id": "c2", "name": "前端", "children": [] } ] }
  ],
  "sites": [
    { "id": "s1", "name": "React", "url": "https://react.dev", "categoryId": "c2", "tags": ["框架"], "status": "ok", "lastCheck": "2026-08-15T10:00:00Z" }
  ],
  "recycleBin": [],
  "tags": []
}
```

### 8.2 持久化要求
- 每次数据变更（增删改、检测结果、回收站、导入导出后）**即时写盘**，刷新/关闭/重启均不丢失
- 文件读写由 Rust 后端通过 Tauri command 完成，前端不直接碰文件系统
- **config.json 保护**：原子写入（临时文件 + rename）；加载解析失败自动改名 `config.json.bak` 后用默认目录继续；配置损坏时视为首次启动

### 8.3 首次启动目录选择
- 应用**首次启动**（config.json 缺失或损坏）显示**应用内引导页**，让用户选择数据目录位置
- 提供"暂不选择，使用默认目录"选项（显示默认路径），之后随时可在设置中迁移
- 所选目录**已有数据文件**（data.json 存在）则先提示，由用户选择读入
- config.json 存在时不再弹出该引导页

### 8.4 备份与恢复
- **覆盖导入**（md 覆盖 / json）前自动备份当前数据为 `.bak`
- **JSON 文件损坏**：自动备份为 `.bak` 后重建空库，避免直接丢数据
- **恢复手段**：a) 回收站恢复单项；b) 导入 md（覆盖式恢复备份）；c) 导入 JSON 备份；d) `.bak` 文件手工替换

---

## 9. 第一版必须完成的功能

1. 网站添加 / 编辑 / 删除 / 回收站（恢复、清空）
2. 多层分类（≤3 层）增删改 + 拖拽；删除分类二选一处理网站
3. 标签输入（自动去重）+ 标签筛选
4. 可访问性检测：检测全部 / 单个 / 批量；2xx/3xx 判定；降级测根域名；断网保护；实时刷新
5. 失效视图 + 批量复查/删除
6. 全局搜索（名称/链接/标签）
7. 侧边栏分类树 / 全部视图 / 失效视图 / 回收站 / 标签筛选
8. 批量操作条 + 右键菜单（检测所选 / 移动分类 / 添加标签 / 删除所选）
9. 双击行 / 悬停 ⋯ 菜单（检测 / 编辑 / 删除 / 移动分类）
10. 本地 JSON 即时持久化；存储位置迁移（跨盘、文件夹选择对话框、非空拒绝、失败回滚）
11. md 导出（含状态标记）+ md 导入（覆盖/合并，忽略状态）+ JSON 导出/导入（完整备份，仅覆盖）
12. 糖果像素视觉风格 + 全部状态设计 + 动效
13. 底部状态栏统计
14. 首次启动数据目录引导页 + config.json 原子写入/损坏恢复
15. 分类创建入口（弹窗内新建 + 侧栏右键添加）+ 应用内统一弹窗（替换 prompt/confirm）
16. 右键菜单位置修复（Teleport + 溢出翻转）+ 弹窗拖拽误关闭修复 + 选中行 hover 稳定

---

## 10. 第一版暂时不做的功能

- 登录注册、联网同步、云数据库
- 启动自动检测、定时检测（仅手动检测）
- 手机端 / 响应式适配
- 网站备注、favicon 图标、排序功能
- 全局标签管理页
- 多语言、主题切换（暗色模式）

---

## 11. 页面结构与布局设计

### 11.1 页面清单（单页面 + 模态层）

| 页面/视图 | 用途 |
|---|---|
| 主视图（全部） | 默认首页，展示所有网站 |
| 分类视图 | 点击分类节点，展示该分类下网站 |
| 失效视图 | 展示失效网站，支持批量清理 |
| 标签视图 | 展示带某标签的网站 |
| 回收站视图 | 展示已删除网站，可恢复/清空 |
| 首次启动引导页 | 首次启动选择数据目录位置（可跳过用默认） |
| 添加/编辑弹窗 | 表单（名称/链接/分类/标签）；分类下拉含"＋ 新建分类" |
| 新建分类弹窗 | 父级选择 + 名称输入（从添加弹窗/侧栏触发） |
| 分类管理弹窗 | 重命名输入 / 删除分类二选一（应用内统一风格） |
| 导入/导出弹窗 | 四按钮（导出/导入 MD/JSON）+ 文件对话框 + md 格式示例 |
| 设置弹窗 | 存储路径显示与迁移、导出/导入入口 |
| 删除确认弹窗 | 删除分类二选一 / 批量删除确认（应用内风格） |

### 11.2 布局结构（四区 + 一栏）

- **顶栏**：Logo（左）→ 搜索框 → 标签筛选下拉 → 检测全部 → ＋添加 → 导入/导出 → 设置（右）
- **侧边栏**：上段分类树（含"全部"）→ 视图区（⚠ 失效）→ 标签区 → 分隔线 → 回收站
- **主区**：批量操作条（选中时出现）→ 检测进度条（检测时出现）→ 数据表格
- **底部状态栏**：总数 / 失效数 / 未检测数 / 上次检测时间

### 11.3 导航与跳转
- 单页内切换，无页面跳转；侧栏点击即切换主区数据
- 模态弹窗为覆盖层，Esc / 点击遮罩可关闭

---

## 12. 交互设计

### 12.1 核心操作

| 操作 | 触发方式 | 步骤 | 结果反馈 |
|---|---|---|---|
| 编辑网站 | 双击行 / 悬停行 ⋯ → 编辑 | 弹出表单 → 保存 | 表格行即时更新，写盘 |
| 单个检测 | 悬停行 ⋯ → 检测 | 行内转圈 → 出结果 | 状态 ♥♥♥/♥ 切换，0.3s 弹跳 |
| 检测全部 | 顶栏"检测全部" | 先探连通性 → 全库逐条检测 | 进度条纹 + 行状态实时刷新 |
| 批量检测 | 勾选多行 → 批量条/右键"检测所选" | 对选中项检测 | 同上，范围限选中 |
| 批量删除 | 勾选多行 → 批量条/右键"删除所选" | 确认 → 删除 | 行折叠淡出，进回收站 |
| 移动分类 | 悬停 ⋯ / 批量条 → 移动分类 | 选目标分类 | 分类列更新 |
| 加标签 | 编辑表单 / 批量条"添加标签" | 输入标签 | 标签列更新 |
| 添加分类 | 添加弹窗下拉"＋新建分类" / 侧栏右键"全部"或分类 | 应用内弹窗选父级输名称 | 创建并自动选中；侧栏即时出现 |
| 重命名分类 | 侧栏分类右键 → 重命名 | 应用内弹窗输入新名称 | 分类名即时更新 |
| 删除分类 | 侧栏分类右键 → 删除 | 应用内弹窗二选一 | 按所选执行 |
| 恢复网站 | 回收站视图 → 恢复 | 点击 | 回到原分类 |
| 清空回收站 | 回收站视图 → 清空 | 确认 → 清空 | 全部移除 |

### 12.2 状态设计

| 状态 | 设计 |
|---|---|
| **空状态** | 无网站时显示："◇ 还没有网站"，提示点「＋ 添加」或「导入/导出」 |
| **加载中** | 检测进行中：顶部进度条纹（紫-粉-紫）+ 行内转圈；数据加载显示骨架/转圈 |
| **错误** | 断网保护提示（奶黄底）："⚠ 检测前连通性探测失败——你的网络似乎断开，本次检测已中止" |
| **成功** | 检测通过：状态变 ♥♥♥（薄荷绿）；保存/导入完成用轻提示 |
| **失效** | 状态 ♥（粉红），行名染红加粗；下次检测通过自动恢复 |
| **未检测** | 状态 ♥?（奶黄） |

---

## 13. 视觉风格设计（糖果像素）

### 13.1 整体视觉调性
像素街机 + 马卡龙糖果色：**奶油亮底、白天游戏杂志的明快感**。活泼、低压力、有记忆点，适合长时间浏览。

### 13.2 配色方案

| 角色 | 色值 | 使用规则 |
|---|---|---|
| 主背景 | `#FFF9FB`（奶油白） | 全局底色 |
| 浅粉底 | `#FFE9F3` | 顶栏/侧栏/状态栏底、行悬停 `#FFF2F7` |
| 主色 | `#7A5CFF`（紫） | 主按钮、选中项、链接、进度条 |
| 辅色 | `#FF5FA8`（粉） | 强调、装饰阴影、次要操作 |
| 文字主 | `#4A3B6E` | 正文 |
| 文字次 | `#B59FD8` | 次级文字、表头 |
| 边框 | `#C9B8E8` | 输入框、分隔 |
| 成功/可访问 | `#4FF0A8`（薄荷绿） | ♥♥♥、可访问标记、标签成功态 |
| 待定/未检测 | `#FFE08A`（奶黄） | ♥?、断网提示底 |
| 危险/失效 | `#FF2D78`（粉红） | ♥、失效行、删除按钮、右键删除项 |

### 13.3 字体规范
- 全站等宽：`'Courier New', monospace`
- 字号层级：标题 22px（可带 1-2px 彩色投影）｜正文 12px ｜次级 11px ｜辅助 10px
- 行高 1.6

### 13.4 间距 / 圆角 / 阴影
- 间距栅格：4px 基准；组件内 8/12px；区块 16/24px
- 圆角：3px（常规）/ 6px（弹窗）/ 999px（标签 pill）
- 阴影：**实色硬阴影**，2-4px 偏移、无模糊；按钮/卡片按下时 `translate(2px,2px)` 去阴影

### 13.5 关键组件样式

| 组件 | 样式 |
|---|---|
| 主按钮 | 紫底 `#7A5CFF` 白字，2px 硬阴影用粉 `#FF5FA8` |
| 次按钮 | 白底，2px 紫/粉边框 + 硬阴影 |
| 危险按钮 | 白底粉框，文字 `#FF2D78` |
| 输入框 | 2px `#C9B8E8` 边框 + 2px 硬阴影 |
| 标签 chip | 白底 + 2px 语义色边框 + 同色硬阴影（框架=薄荷/失效=粉/待查=奶黄） |
| 状态表达 | 心数：♥♥♥ 可访问 / ♥ 失效 / ♥? 未检测 |
| 复选框 | 2px `#C9B8E8` 直角框；选中紫底白 ✓ |
| 右键菜单 | 白底 2px 边框 4px 硬阴影，项悬停浅粉，删除项粉红 |
| 侧栏选中 | 紫底白字 + 粉色硬阴影 |

### 13.6 动效
- 按下反馈：按钮/卡片 active `translate(2px,2px)` 去阴影
- 悬停：行变浅粉 `#FFF2F7`；侧栏项变奶黄底
- 进入：整窗加载，侧栏/表格/状态栏依次从下方 8px 淡入（0.2s，stagger 80ms）
- 检测：进度条紫-粉-紫条纹前进；单行状态切换 0.3s 弹跳
- 菜单：0.15s 快速淡入 + 3px 上移
- 删除：行高 0.2s 收缩折叠淡出

---

## 14. 方案确认记录（先布局交互、后视觉风格）

### 14.1 第一阶段：布局与交互设计
提供了多套布局方案，最终选定**表格化 + 多选右键交互**：

| 轮次 | 提供的备选 | 结果 |
|---|---|---|
| 布局初稿 | ① 三栏（分类树/列表/常驻详情）② 两栏（分类树/列表+内嵌详情）③ 单栏流式 | ❌ 三套均否，要求"中间变类表格" |
| 表格化布局 | ① 表格+底部抽屉 ② 表格+右侧常驻面板 ③ 表格+行内展开 | ❌ 用户要求给**完整页面**（含检测/回收站等） |
| 完整布局 | 整窗布局（顶栏/侧栏/表格/状态栏）+ 检测视图 + 4 弹窗 | ✅ 布局通过，但要求**去掉常驻操作列** |
| 多选交互 | 行首复选框 + 批量操作条 + 右键菜单 | ✅ 通过，并确认：批量条与右键**都保留**；编辑用**双击行 + 悬停 ⋯**（去掉显式按钮）；新增**失效视图**入口 |

### 14.2 第二阶段：视觉风格设计
| 轮次 | 提供的备选 | 结果 |
|---|---|---|
| 风格大类 v1 | 极简现代 / 科技暗色 / 自然温暖 / 商务专业 / 复古工整 / 活力多彩 | ❌ 均被否（"都好丑"） |
| 风格大类 v2 | 档案索引卡 / 终端控制台 / 编辑杂志 / 墨纸和风 / 像素街机 | ✅ 选定 **像素街机** |
| 像素小类 | ① 街机厅霓虹 ② 掌机绿屏 ③ 合成波日落 ④ 糖果像素 | ✅ 选定 **④ 糖果像素**（奶油亮底 + 马卡龙色 + 心数状态） |

---

## 15. 验收标准（可逐页逐项实际检查）

### 15.1 布局验收
- [ ] 侧边栏含：分类树（≤3 层可折叠）/ ⚠ 失效视图 / 标签列表 / 回收站 / 未分类
- [ ] 顶栏含：搜索框 / 标签筛选 / 检测全部 / ＋添加 / 导入/导出 / 设置
- [ ] 主区表格列：复选框 / 名称 / 链接 / 分类 / 标签 / 生命状态（无操作列）
- [ ] 底部状态栏：总数 / 失效数 / 未检测数 / 上次检测时间
- [ ] 单页应用，无页面跳转；弹窗为模态层，Esc 可关

### 15.2 交互验收
- [ ] 双击行打开编辑；悬停行出现 ⋯ 菜单（检测/编辑/删除/移动）
- [ ] 勾选多行出现顶部批量条 + 右键菜单；Esc 或 ✕ 取消选择
- [ ] 检测全部 → 进度条 + 行状态实时刷新，不阻塞其他操作
- [ ] 失效标红（♥）；下次检测通过自动恢复（♥♥♥）
- [ ] 断网保护：连通性失败先提示再中止，不批量误标
- [ ] 链接唯一：重复链接添加被拦截
- [ ] 选中行 hover 保持紫色稳定，不闪烁变色
- [ ] 弹窗内拖选文字拖出弹窗松开不关闭；仅点击遮罩空白关闭
- [ ] 右键菜单紧贴鼠标位置，靠近窗口边缘自动翻转不溢出
- [ ] 删除分类弹框二选一；删除均进回收站；回收站内不参与检测
- [ ] 导入弹框选覆盖/合并；覆盖前自动备份 .bak

### 15.4 新增功能验收（v1.1）
- [ ] 跨盘迁移：数据目录可迁到其他盘符；目标目录非空时拒绝并提示；迁移成功旧文件不残留
- [ ] 首次启动引导页：config 缺失时弹出，可选目录或跳过用默认；已有数据时提示并可选读入
- [ ] config.json 损坏自动 .bak 恢复并重新走首次启动流程
- [ ] 设置弹窗显示数据文件完整路径（含文件名）
- [ ] 导入/导出四按钮齐全（MD/JSON × 导入/导出），均走系统文件对话框
- [ ] 导出保存对话框预填 `网站收藏_<日期>.md/.json`；导出成功状态栏闪现提示
- [ ] JSON 导出内容为完整备份；JSON 导入仅覆盖 + 自动 .bak + 弹确认
- [ ] md 导入弹窗显示格式示例；JSON 导入显示说明文字
- [ ] 添加弹窗分类下拉含"＋ 新建分类"，创建后自动选中
- [ ] 侧栏右键"全部"添加顶层分类；右键分类添加子分类；第 3 层分类不显示"添加子分类"
- [ ] 全应用无浏览器原生 prompt/confirm，统一应用内弹窗

### 15.3 视觉验收（可量化）
- [ ] 主背景 `#FFF9FB`；顶栏/侧栏底 `#FFE9F3`
- [ ] 主色 `#7A5CFF`、辅色 `#FF5FA8`；成功 `#4FF0A8`、待定 `#FFE08A`、危险 `#FF2D78`
- [ ] 全站等宽字体 Courier New；正文 12px、次级 11px、辅助 10px；行高 1.6
- [ ] 间距为 4px 基准（4/8/12/16/24）
- [ ] 圆角：常规 3px / 弹窗 6px / 标签 999px
- [ ] 阴影为实色硬阴影 2-4px 无模糊；按钮按下位移 2px 去阴影
- [ ] 状态表达：♥♥♥ / ♥ / ♥? 三态齐全
- [ ] 动效：进入淡入 stagger、检测条纹进度、状态切换 0.3s 弹跳、删除 0.2s 折叠

---

## 附：md 交换格式（导出/导入）

```markdown
# 开发工具
## 前端
- [React](https://react.dev) ✅ 2026-08-15
- [Vue](https://vuejs.org) ❌ 2026-08-15
# 设计资源
- [Dribbble](https://dribbble.com) ✅ 2026-08-15
```

- `#`/`##`/`###` = 一/二/三级分类；`- [名称](链接)` = 网站
- 导出：带状态标记（✅/❌ + 日期）
- 导入：忽略状态标记（视为未检测）；标签不进入 md
- 导入弹窗展示此格式示例（"导入的 md 格式"）

## 附：JSON 交换格式（导出/导入）

- 导出/导入的是**完整数据序列化**（`AppData` 结构，含 categories / sites / recycleBin / tags）
- 与 `data.json` 存储格式一致；用于跨设备完整备份与恢复
- 导入仅覆盖模式：清空当前数据后载入文件内容；覆盖前自动备份当前数据为 `.bak`；执行前弹确认
- 导入弹窗显示说明："导入程序导出的 .json 备份文件"